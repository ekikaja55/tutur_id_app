import 'package:flutter/material.dart';

class TuturApp extends StatelessWidget {
  const TuturApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
